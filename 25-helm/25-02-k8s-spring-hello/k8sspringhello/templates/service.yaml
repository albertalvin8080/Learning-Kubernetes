apiVersion: v1
kind: Service
metadata:
  name: {{ include "k8sspringhello.fullname" . }}
  labels:
    {{- include "k8sspringhello.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "k8sspringhello.selectorLabels" . | nindent 4 }}
